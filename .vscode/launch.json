{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Build & Debug Current C File",
      "type": "cppdbg",
      "request": "launch",
      "program": "${fileDirname}/${fileBasenameNoExtension}",
      "cwd": "${fileDirname}",
      "MIMode": "gdb",
      "miDebuggerPath": "/usr/bin/gdb",
      "preLaunchTask": "Build Current C File",
      "externalConsole": false
    }
  ]
}
